# {{name}}

A Cargo workspace. Each crate lives in `crates/`; `cargo new crates/<name>`
(or `SPC p c` with the folder set to `crates`) adds one.

```
cargo build --workspace
cargo test --workspace
```
