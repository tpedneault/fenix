# {{name}}

A command-line tool, managed by [uv](https://docs.astral.sh/uv/).

```
uv run {{name_kebab}} Ada           # run it
uv tool install --editable .        # put `{{name_kebab}}` on your PATH
uv run pytest                       # the tests
```
