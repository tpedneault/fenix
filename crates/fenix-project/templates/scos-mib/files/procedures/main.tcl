# {{name}} -- telecommand procedures for {{label}}.
#
# In a Tcl file, SPC m i builds a telecommand from the MIB and inserts it
# at the cursor.

namespace eval {{name_snake}} {
    proc main {} {
    }
}
